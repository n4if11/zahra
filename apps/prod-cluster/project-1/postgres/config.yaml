appName: "" # If Empty the Application name will be "prod-cluster-postgres"
destNamespace: "" # If Empty the Application destination namespace will be "postgres"
destServer: "" # If Empty the Application destination server will be "prod-cluster"
repoURL: "" # If Empty the Application source repo will be this repo
srcPath: "" # If Empty the Application source path will be this file's path "apps/prod-cluster/project-1/postgres"
srcTargetRevision: "" # If Empty the Application source target revision will be "HEAD"
labels: {} # Additional Application labels
annotations: {} # Additional Application annotations
noAutoSync: "true" # Set to "true" to disable automated sync for this app
