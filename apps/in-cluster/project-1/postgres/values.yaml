postgres:
  settings:
    superuser:
      secretKey: user
      value: postgres_admin
    superuserPassword:
      secretKey: password
      value: admin@123
      
  userDatabase:
    name:
      secretKey: user
      value: user
    user:
      secretKey: user
      value: user
    password:
      secretKey: password
      value: admin@123