postgres:
  settings:
    superuser:
      secretKey: user
      value: postgres_admin
    superuserPassword:
      secretKey: password
      value: admin@123
      
  userDatabase:
    name:
      secretKey: postgres
      value: user
    user:
      secretKey: user
      value: user
    password:
      secretKey: password
      value: admin@123